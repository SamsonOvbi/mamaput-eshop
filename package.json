{
  "name": "mamaput-eshop",
  "version": "0.0.0",
  "scripts": {
    "ng": "ng",
    "start": "cd backend/build && node index.js",
    "prebuild": "cd backend && yarn run build",
    "build": "cd frontend && yarn run build"
  },
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}