{
  "name": "mamaput-eshop",
  "version": "0.0.0",
  "engines": {
    "node": "<=18 <21"
  },
  "scripts": {
    "ng": "ng",
    "start": "cd backend/build && node index.js",
    "prebuild": "cd backend && yarn run build",
    "postbuild": "cd frontend && yarn run build"
  },
  "private": true,
  "dependencies": {},
  "devDependencies": {
  }
}